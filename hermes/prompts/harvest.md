The day is closed. Before the lights go out: bank what today's work taught,
so tomorrow's shift doesn't relearn it the hard way.

Read the day's reports below. If they contain a reusable procedure or a
hard-won gotcha:
- `write_skill` a new skill, or update an existing one (`load_skill` first,
  then `write_skill` with the same name to edit in place).
- First line of the content is the one-line description; the rest is the full
  procedure — the specific commands, the exact error, how it was beaten.
- At most one or two skills. If nothing here is genuinely reusable, say so in
  one line and stop — never invent a skill for the sake of it.
